rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read for all images
    match /{allPaths=**} {
      allow read: if true;
      // Only authenticated users (admins/editors) can upload
      allow write: if request.auth != null;
    }
  }
}
